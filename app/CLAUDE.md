# INSTRUCCIONES PARA CLAUDE CODE

## Proyecto
- React + Vite + Tailwind CSS
- Atomic Design: atomos → moleculas → organismos → templates
- La migración styled-components → Tailwind CSS ya terminó (ver `CLAUDE.md` en la raíz del repo) — no hace falta escribir archivos de estado de sesión ni leer ninguno al empezar.